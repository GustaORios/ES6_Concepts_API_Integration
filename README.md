# 📘 Student Grade Tracker

A simple web app to register students, record their grades, calculate the average, and fetch sample user data from an external API.

## ✨ Features

- Add students with name and grade
- Prevent duplicate student entries
- Display a list of registered students
- Calculate and show the average grade
- Save student data to `localStorage`
- Fetch and display user data from https://jsonplaceholder.typicode.com/users

## 🛠 Technologies Used

- HTML5  
- CSS (with Bootstrap)  
- JavaScript (DOM manipulation, Fetch API, localStorage)  
